{
	method drive(string "f");
	waituntil (method getSonars(int 1) < int 20);
	method stop();
	
	method turnDegrees(int 90);
	
	method drive(string "f");
	waituntil (method getSonars(int 1) < int 20);
	method stop();
}